services:
  postgres:
    image: postgres:17
    container_name: itc6050_pg
    environment:
      POSTGRES_USER: itc6050
      POSTGRES_PASSWORD: itc6050
      POSTGRES_DB: lab
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: